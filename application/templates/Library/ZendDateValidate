<?php 
/**
 * {$brandName}
 *
 * {$description}
 *
 * @category   Project
 * @package    Project_Validator
 * @copyright  {$copyright}
 * @author     zetta
 * @version    {$version} SVN: $Id$
 */

/**
 * Interfaz Validator
 * @category   Project
 * @package    Project_Validator
 * @subpackage Project_Validator_Validates
 * @copyright  {$copyright}
<block:benderSignature>
 * @copyright  {$benderSignature}
 * @author     zetta 
</block:benderSignature>
 * @version    {$version} SVN: $Revision$
 */
class ZendDateValidate extends Zend_Validate_Abstract
{
    const DATE = 'date';

    protected $_messageTemplates = array(
        self::DATE => "'%value%' is not a zend date"
    );

    /**
     * Check the value
     */
    public function isValid($value)
    {
        $this->_setValue($value);
        if (!($value instanceof Zend_Date)) {
            $this->_error();
            return false;
        }
        return true;
    }
}